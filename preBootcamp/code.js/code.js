// on insialized this is varriable likecount of zero 
var likecount = 0;
// on create a function increaselikes for calculate or sum  numbers at likes for flowers 
function increaselikes() {
    // on increment one at this varriable likecount
    likecount = likecount + 1;
}

